name: CI to Build and Push Docker Image

on:
  push:
    branches:
      - "main"
      - "dev"
  workflow_dispatch:

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Get Commit Hash
        id: vars
        run: echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Determine Image Tag
        run: |
          if [[ "${{ github.ref_name }}" == "main" ]]; then
            echo "IMAGE_TAG=actions-latest" >> $GITHUB_ENV
          elif [[ "${{ github.ref_name }}" == "dev" ]]; then
            echo "IMAGE_TAG=dev-latest" >> $GITHUB_ENV
          fi

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Image
        run: |
          echo "Building and pushing Docker image for $IMAGE_TAG..."
          docker build -t opacitylabs/opacity-node-selector:$IMAGE_TAG -f Dockerfile .
          docker push opacitylabs/opacity-node-selector:$IMAGE_TAG
